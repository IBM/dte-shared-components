import CalendarMinToolbar from "./CalendarMinToolbar";

export default {
  title: "DTE/CalendarMinToolbar",
  component: CalendarMinToolbar,
};

const Template = (args) => <CalendarMinToolbar {...args} />;

export const Default = Template.bind({});
Default.args = {};
