import ConfirmModal from "./ConfirmModal";

export default {
  title: "DTE/ConfirmModal",
  component: ConfirmModal,
};

const Template = (args) => <ConfirmModal {...args} />;

export const Default = Template.bind({});
Default.args = {};
