import NumberInput from "./NumberInput";

export default {
  title: "DTE/NumberInput",
  component: NumberInput,
};
const Template = (args) => <NumberInput {...args} />;

export const Default = Template.bind({});
Default.args = {};
