import TextInput from "./TextInput";

export default {
  title: "DTE/TextInput",
  component: TextInput,
};

const Template = (args) => <TextInput />;

export const Default = Template.bind({});
Default.args = {};
