import EditPageButton from "./EditPageButton";

export default {
  title: "DTE/EditPageButton",
  component: EditPageButton,
};

const Template = (args) => <EditPageButton {...args} />;

export const Default = Template.bind({});
Default.args = {};
