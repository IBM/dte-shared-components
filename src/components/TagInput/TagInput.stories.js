import TagInput from "./TagInput";

export default {
  title: "DTE/TagInput",
  component: TagInput,
};

const Template = (args) => <TagInput />;

export const Default = Template.bind({});
Default.args = {};
