import TextArea from "./TextArea";

export default {
  title: "DTE/TextArea",
  component: TextArea,
};

const Template = (args) => <TextArea />;

export const Default = Template.bind({});
Default.args = {};
